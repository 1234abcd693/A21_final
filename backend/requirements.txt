# A21 后端依赖
# 安装: pip install -r requirements.txt
# Python 3.10+

# Web 框架 (FastAPI + 异步服务器)
fastapi==0.115.0
uvicorn[standard]==0.30.0

# RAG 框架
langchain==0.3.0
langchain-community==0.3.0

# 向量数据库
chromadb==0.5.0

# 图数据库客户端
neo4j==5.25.0

# NLP 工具
jieba==0.42.1
sentence-transformers==3.1.0

# 检索算法
rank-bm25==0.2.2

# 文档解析
python-docx==1.1.0
pdfplumber==0.11.0

# HTTP 客户端 (前端通信、健康检查)
httpx==0.27.0

# 文件上传支持
python-multipart==0.0.9

# 磁盘缓存 (问答结果缓存，提升性能)
diskcache==5.6.3

# 配置管理
python-dotenv==1.0.0

# 语音识别 (Vosk HTTP)
vosk==0.3.45

# 密码加密 (优先使用)
bcrypt==4.2.0

# 打包工具 (开发环境)
pyinstaller==6.11.0
