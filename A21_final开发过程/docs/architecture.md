# A21 系统架构设计

> 版本: v0.2 | 分支: Plan | 状态: 讨论中

---

## 一、分层架构

```
┌─────────────────────────────────────────────────────┐
│  1. 前端交互层                                       │
│     · 聊天界面（流式 SSE，对话气泡）                   │
│     · 知识图谱可视化（Neo4j → 力导向图）              │
│     · 文档上传 + 知识抽取预览                         │
│     · 反馈按钮（👍/👎）                              │
│     · 数据导入/导出操作入口                           │
│     候选: Streamlit / Flask+HTML / Electron+Vue3     │
├─────────────────────────────────────────────────────┤
│  2. 服务层（FastAPI）                                │
│     · 路由分发与请求验证                              │
│     · SSE 流式响应管理                                │
│     · 中间件（错误处理、日志）                         │
│     · 服务生命周期（启动 llm-server、Neo4j）          │
├─────────────┬──────────────┬────────────────────────┤
│ 3. RAG 层   │ 4. 反馈层     │ 5. 数据层               │
│             │              │                         │
│ ┌检索模块┐  │ · 反馈收集    │ ┌─ 知识图谱 (Neo4j)    │
│ │BM25    │  │ · 数据存储    │ ├─ 向量库 (Chroma)     │
│ │向量检索│  │ · 参数优化    │ ├─ 对话历史            │
│ │融合排序│  │ · 效果评估    │ └─ 上下文管理          │
│ ├────────┤  │              │                         │
│ └生成模块┐  │              │                         │
│ │Prompt  │  │              │                         │
│ │LLM 调用│  │              │                         │
│ │答案验证│  │              │                         │
│ └────────┘  │              │                         │
├─────────────┴──────────────┴────────────────────────┤
│  6. 工具层                                           │
│     · 知识抽取（文档解析 → 实体抽取 → 候选生成）       │
│     · 数据清洗（格式标准化、去重）                     │
│     · 数据同步（打包导出 / 解包导入 / 冲突合并）       │
│     · 报告生成（Word 导出问答记录）                    │
└─────────────────────────────────────────────────────┘
```

---

## 二、各层职责与接口

### 2.1 前端交互层

| 职责 | 说明 |
|------|------|
| 聊天界面 | 输入框 + 流式气泡 + 引用悬浮（[1] 显示来源） |
| 图谱可视化 | D3.js / vis.js 渲染力导向图，点击节点展开 |
| 反馈交互 | 每条回答下方 👍/👎 按钮，可选文字反馈 |
| 管理界面 | 文档上传、数据同步、参数面板 |

### 2.2 服务层

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/v1/ask` | POST | 问答（SSE 流式返回） |
| `/api/v1/symptoms` | GET | 故障现象联想（输入框自动补全） |
| `/api/v1/parse` | POST | 上传文档，返回抽取候选 |
| `/api/v1/confirm` | POST | 确认导入候选到 KG |
| `/api/v1/feedback` | POST | 提交 👍/👎 |
| `/api/v1/export` | GET | 导出数据包（zip） |
| `/api/v1/import` | POST | 导入数据包 |
| `/api/v1/history` | GET | 历史对话列表 |
| `/api/v1/report` | GET | 导出问答报告 |

### 2.3 RAG 层

**检索模块**：
```
用户问题 → 关键词提取 → BM25 检索 ─┐
           → 向量化     → 向量检索 ─┤→ 融合排序(α·BM25 + β·向量) → Top-K 片段
```

**生成模块**：
```
Top-K 片段 + 用户问题 + 对话历史 → Prompt 模板 → llama-server → SSE 流式输出

生成完毕后 → 答案验证：
  · 引用完整性检查（[N] 是否在检索范围内）
  · 事实一致性检查（答案关键名词是否在源文档中出现）
  · 低可信警告标记
```

### 2.4 反馈层

```
用户评价 → SQLite 存储 → (积累 N 条后/手动触发) → 参数优化算法 → 更新 RAG 参数
```

| 待定 | 选项 |
|------|------|
| 调优对象 | BM25/向量权重(α,β)、Top-K 值、重排序策略 |
| 调优算法 | 网格搜索 / 贝叶斯优化 / 遗传算法 |
| 触发方式 | 手动触发 / 自动(阈值触发) |

### 2.5 数据层

| 子模块 | 存储 | 职责 |
|--------|------|------|
| 知识图谱 | Neo4j | 实体关系持久化、Cypher 查询 |
| 向量库 | Chroma | 文档切片 + embedding 存储和检索 |
| 对话历史 | SQLite / JSON | 按会话组织，支持分页查询 |
| 上下文管理 | 内存 | 当前会话的多轮对话上下文窗口 |

### 2.6 工具层

| 工具 | 输入 | 输出 |
|------|------|------|
| 文档解析 | Word/PDF/TXT | 纯文本 |
| 知识抽取 | 纯文本 | 候选三元组（现象-原因-步骤） |
| 数据清洗 | 原始数据 | 标准化数据（去重、格式统一） |
| 数据导出 | — | zip（Neo4j + Chroma + SQLite + 配置） |
| 数据导入 | zip | 合并后的系统数据 |

---

## 三、待决策事项

| # | 决策 | 状态 |
|---|------|------|
| 1 | 前端框架：Streamlit / Flask / Electron？ | 🔴 待讨论 |
| 2 | LLM 模型：Qwen2-3B / Qwen2-7B / GLM-4-9B？ | 🔴 待讨论 |
| 3 | 知识抽取方式：纯正则 / 小模型 NER / 两者结合？ | 🔴 待讨论 |
| 4 | 反馈调优算法选型 | 🔴 待讨论 |
| 5 | U盘同步冲突策略 | 🔴 待讨论 |
| 6 | 前端是否需要 Electron（桌面应用）还是 Web 即可？ | 🔴 待讨论 |

---

## 四、版本记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-05-22 | v0.1 | 初始 6 层草案 |
| 2026-05-22 | v0.2 | 合并 API 层、新增反馈层、明确防幻觉位置、展开子模块 |
