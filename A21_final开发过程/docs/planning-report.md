# A21 项目规划报告

> 版本: v2.0 (FINAL) | 分支: Plan | 日期: 2026-05-22  
> 整合截至当前的所有架构设计讨论与决策

---

## 一、项目概述

**赛题**：中国大学生服务外包创新创业大赛 A21  
**命题**：大连海事大学  
**项目**：船舶装备故障诊断智能问答系统  
**核心路线**：RAG（检索增强生成） + 本地小模型 + 离线运行  

---

## 二、已确认的技术决策

### 2.1 系统架构（6 层）

```
┌──────────────────────────────────────────────┐
│ 1. 前端交互层                                 │
│    聊天界面(SSE流式) / 图谱可视化 / 文档上传    │
│    反馈按钮(👍👎) / 数据同步入口               │
├──────────────────────────────────────────────┤
│ 2. 服务层 (FastAPI)                          │
│    路由分发 / 请求验证 / SSE 流式推送          │
├──────────┬──────────────┬────────────────────┤
│ 3. RAG层 │ 4. 反馈层     │ 5. 数据层           │
│ 检索模块 │ 收集/存储     │ 知识图谱(Neo4j)     │
│ 生成模块 │ 参数优化      │ 向量库(Chroma)      │
│ 答案验证 │              │ 对话历史/上下文      │
├──────────┴──────────────┴────────────────────┤
│ 6. 工具层                                     │
│    知识抽取 / 文档解析 / 数据清洗               │
│    U盘同步(导入导出) / 报告生成                 │
└──────────────────────────────────────────────┘
```

### 2.2 模型选型

| 项 | 选择 | 理由 |
|----|------|------|
| 模型 | **Qwen2.5-1.5B-Instruct** | ≤7B 限制下唯一满足 3s TTFT + 8GB 内存约束 |
| 量化 | Q4_K_M（~1 GB） | 最佳速度/质量平衡，普通 CPU 可流畅运行 |
| 推理引擎 | **llama.cpp**（llama-server 模式） | C++ 原生性能，AVX2 加速，支持 prompt 缓存 |
| 优化 | AVX2 + 4线程 + mlock + flash-attn | 最大化 CPU 推理速度 |

### 2.3 检索策略（三路融合）

```
用户问题 → jieba分词 → NER实体抽取
    │
    ├── 实体名 → Neo4j Cypher → 图检索结果（结构化三元组）
    ├── 关键词 → BM25 检索    → BM25 结果（精确匹配片段）
    └── Embedding → Chroma   → 向量结果（语义相似片段）
    │
    └── RRF 融合 → Reranker 重排序 → Top-5 注入 Prompt
```

| 检索方式 | 工具 | 职责 |
|----------|------|------|
| 图检索 | Neo4j + Cypher | 实体关系推理、因果链 |
| 向量检索 | Chroma + MiniLM | 语义相似、同义词覆盖 |
| BM25 | rank_bm25 | 精确术语/数值匹配 |
| 融合 | RRF（先）→ 加权（后，反馈驱动） | 三路结果统一排序 |
| 重排序 | Cross-encoder Reranker | 精细排序，提升 Top-5 精度 |

### 2.4 NLP 技术栈

| 环节 | 技术 | 说明 |
|------|------|------|
| 分词 | jieba | 中文分词基础 |
| 实体识别 | 字典匹配 + jieba 词性标注 | 提取设备名、零件名、故障类型 |
| Query 改写 | 同义词扩展 / LLM 辅助 | "打不着火"→"无法启动" |
| 文本分块 | 滑动窗口 256-512 token + 10-20% 重叠 | 平衡检索精度和上下文完整性 |
| Embedding | paraphrase-multilingual-MiniLM-L12-v2 | 384 维，中英双语，轻量 |
| 答案验证 | jieba 关键词 + 源文档匹配 | 防幻觉，低可信警告 |

### 2.5 防幻觉机制

```
生成答案 → 提取关键名词(jieba) → 检查是否在检索片段中出现
                                    ├─ 全部出现 → 正常输出
                                    └─ 有未出现 → 标记 "⚠️ 部分信息未经源文档确认"
```

### 2.6 Prompt 优化

- **Prompt 缓存**：系统指令永久缓存，每次只处理新内容（节省 ~50% TTFT）
- **结构化输出**：固定格式（原因列表 + 维修步骤 + 引用编号 + 注意事项）
- **上下文窗口**：≤ 2048 token，确保内存和速度

---

## 三、Git 分支策略

```
main ────────────────────────────→ 最终交付
  ├── Plan ──────────────────────→ 设计文档（当前）
  └── dev ───────────────────────→ 集成测试
      ├── feat/frontend ─────────→ 前端
      ├── feat/backend ──────────→ 服务层
      ├── feat/rag ──────────────→ RAG 层
      ├── feat/kg ───────────────→ 知识图谱
      ├── feat/tools ────────────→ 工具层
      └── feat/feedback ─────────→ 反馈层
```

---

## 四、待决策事项

| # | 决策 | 状态 | 优先级 |
|---|------|------|--------|
| 1 | 前端框架 | ✅ **Electron + Vue 3 + Vite**（Windows 桌面应用） |
| 2 | 知识抽取方式 | ✅ **混合方案**：正则(70%) + 1.5B few-shot(25%) + 手工兜底(5%)；前端图谱预览确认 |
| 3 | 反馈调优算法 | ✅ **网格搜索**（α/β/γ + Top-K），模块化设计，后续可换贝叶斯 |
| 4 | U盘同步 | ✅ **Cypher 导出 + MERGE 导入**（Neo4j）；目录拷贝（Chroma）；hash 去重；参数用户选择 |
| 5 | Embedding 模型 | ✅ **bge-base-zh-v1.5**（768维/410MB） |
| 6 | Reranker | ✅ **暂不上**（三路检索+RRF 后评估） |
| 7 | 知识图谱本体 | ✅ 同伴 `知识图谱设计文档.md` v4.0（7节点8关系，`import.cypher` 初始数据） |

---

## 五、关于 C++ 的说明

项目标记为"Python + C++"，但 C++ 部分**不需要你写代码**：

| 是什么 | 角色 | 你需要做什么 |
|--------|------|------------|
| llama.cpp | C++ 写的 LLM 推理引擎 | 下载预编译的 `llama-server.exe`，命令行启动，Python 通过 HTTP 调用 |
| 编译 | 已经由 llama.cpp 项目组完成了 | 不需要装 CMake、不需要编译 |

> llama.cpp 之于本项目，就像 Chrome 之于 Selenium 自动化测试——你用它，但不改它。

---

## 六、桌面应用打包

| 组件 | 工具 | 产物 |
|------|------|------|
| Python 后端 | PyInstaller | `backend.exe` |
| 前端 | Vite + electron-builder | `A21 船舶故障诊断系统 Setup.exe` |
| llama.cpp | 预编译 | `llama-server.exe`（直接放进 resources） |
| 模型 | — | `model/*.gguf`（直接放进 resources） |
| Neo4j | 绿色版 | `neo4j/`（内置 JRE，直接放进 resources） |

详见 `docs/packaging-guide.md`。用户双击 exe → 自动启动 Neo4j + llama-server + 后端 → 打开窗口。

---

## 七、性能约束与应对

| 约束 | 应对措施 |
|------|----------|
| ≤3s 响应 | 1.5B 模型 + Prompt 缓存 + 压缩上下文 + AVX2 优化 |
| ≤8GB 内存 | Q4_K_M 量化(~1GB) + KV cache 8bit + 精简依赖 |
| 完全离线 | llama.cpp + 本地 Neo4j + 本地 Chroma + 无网络依赖 |
| ≥500 实体 | 赛题数据 + 知识抽取补充 + 手工录入 |
| ≥75% Top1 准确率 | 三路检索融合 + Reranker + 结构化 Prompt + 答案验证 |

---

## 八、相关文档

| 文档 | 位置 |
|------|------|
| 需求规格 | `docs/requirements.md` |
| 架构设计 | `docs/architecture.md` |
| 协作流程 | `docs/github-workflow.md` |
| 环境搭建 | `docs/dev-env-setup.md` |
| Neo4j 指南 | `docs/neo4j-guide.md` |
| 测试方案 | `docs/testing-plan.md` |
| 前端设计 | `docs/frontend-design.md` |
| 依赖清单 | `../backend/requirements.txt` |
| 知识图谱设计 | `../知识图谱设计文档.md` |
| KG 初始数据 | `../import.cypher` |
| 知识库(24篇) | `知识库/` |
| 需求追踪矩阵 | `docs/traceability-matrix.md` |

---

## 七、版本记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-05-25 | v2.2 | 新增需求-功能-模块追踪矩阵，知识库扩至24篇 |
